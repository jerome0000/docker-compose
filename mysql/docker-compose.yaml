version: '3'
services:
  mysql:
    image: mysql:5.7.23
    restart: always
    container_name: "mysql5.7"
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - 3306:3306
    volumes:
      - ./data:/var/lib/mysql